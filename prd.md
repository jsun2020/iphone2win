# iphone2win 产品需求文档

## 1. 文档信息

- 产品：iphone2win
- 文档状态：基于当前代码的产品基线
- 更新日期：2026-07-12
- 目标平台：Windows、iPhone/iPad

## 2. 产品概述

iphone2win 是面向 Apple 移动设备与 Windows 电脑的本地文件传输工具。产品解决 iPhone/iPad 与 Windows 之间缺少统一、直接、无需账号的文件交换入口这一问题，并提供三类互补路径：

1. 同一局域网内的应用间点对点传输；
2. 无需在 iPhone 端预先打开配对页面的二维码浏览器传输；
3. 无网络环境下的手动或自动 USB 数据线传输。

本项目基于 LocalSend 开发，但产品范围聚焦于 iPhone/iPad 与 Windows，不提供互联网中继、云盘或账号体系。

## 3. 目标用户与场景

### 3.1 目标用户

- 同时使用 iPhone/iPad 和 Windows PC 的个人用户；
- 需要在内网或离线环境传输文件的用户；
- 不希望文件经过云端、第三方服务器或账号系统的隐私敏感用户；
- 需要便携软件、无法在 Windows 长期安装客户端的用户。

### 3.2 核心场景

- 用户在同一 Wi-Fi 下将照片、文档或文本从 iPhone 发送到 Windows，或反向发送。
- iPhone 端不方便安装或操作应用时，扫描 Windows 上的二维码，通过浏览器上传或下载。
- 无 Wi-Fi、网络隔离或网络质量差时，通过 USB 数据线和 Apple 文件共享交换文件。
- Windows 用户希望连接并信任 iPhone 后，直接在 iphone2win 中推送或拉取应用文件共享目录中的文件。

## 4. 产品目标

### 4.1 目标

- 覆盖 iPhone/iPad 与 Windows 的双向文件和文本传输。
- 在局域网、浏览器和 USB 三种路径之间提供清晰的降级方案。
- 文件内容不经过第三方服务器，默认不产生与传输无关的公网连接。
- 复用统一的接收确认、进度、文件保存、历史和 PIN 能力，降低行为差异。
- 保持 Windows 便携部署能力，自动 USB 依赖缺失时不影响其他功能。

### 4.2 非目标

- 公网传输、NAT 穿透、STUN/TURN、服务器中继或 VPS 模式；
- 账号、联系人、跨设备云同步、远程文件管理；
- 后台剪贴板同步；
- 将 iPhone 映射为完整 Windows 磁盘；
- 读取照片/DCIM、其他 iOS 应用容器或系统目录；
- 自动下载 Apple 驱动或 `libimobiledevice` 工具；
- 遥测、用户行为分析、广告、应用内购买或捐赠功能。

## 5. 功能需求

### 5.1 局域网发现与直接传输（P0）

**需求**

- 两端应用在同一局域网时，通过本地组播发现附近设备。
- 用户可选择文件、目录、媒体或文本并发送给目标设备。
- 接收端支持接受、拒绝、自动接收、接收 PIN、进度展示、目标目录保存和接收历史。
- 文件字节通过设备本地地址直接传输，不经过第三方服务。

**验收标准**

- iPhone/iPad 与 Windows 能互相发现并完成双向传输。
- 接收拒绝、PIN 错误、会话冲突和传输失败均有明确结果。
- 应用启动、发现和传输期间不连接公网信令、STUN、TURN、遥测或购买服务。

### 5.2 iPhone 扫码上传到 Windows（P0）

**需求**

- Windows 接收页在本地服务器运行且存在局域网 IP 时提供二维码入口。
- 二维码内容为 `http://<局域网 IP>:<端口>/upload`，不包含设备别名、型号、版本、令牌、指纹或公网 IP。
- iPhone 浏览器可选择一个或多个文件并上传。
- 上传复用现有 v2 接收接口和接收流程，不建立独立的绕过确认保存通道。
- 网页资源随应用本地提供，不请求任何远程资源。

**验收标准**

- 扫码后可加载本地页面、选择文件并完成上传。
- 普通模式下 Windows 显示接受/拒绝请求；自动接收和 PIN 按现有设置生效。
- 无可用 IP 或服务器未运行时，二维码入口不可用或给出明确提示。
- 接收端忙、拒绝或上传失败时，浏览器显示可理解的错误。

### 5.3 Windows 扫码分享给 iPhone（P0）

**需求**

- Windows 发送页选择文件或文本后提供直接二维码分享入口。
- iPhone 浏览器可查看文件列表并逐项下载。
- 文本内容应显示预览，并提供复制按钮；不支持 Clipboard API 时提供兼容回退。

**验收标准**

- 未选择内容时不创建空分享会话，并提示用户先选择内容。
- iPhone 扫码后可下载选定文件。
- 每个文本项均显示对应内容并可复制，不错误复用首个文本项。

### 5.4 手动 USB 文件交换（P0）

**需求**

- iOS 应用开启 Apple File Sharing 和原位打开文档能力。
- 应用 Documents 下维护 `USB-Inbox` 与 `USB-Outbox`。
- 用户可将发送页选定文件或剪贴板文本导出到 `USB-Outbox`。
- USB 页面展示两个目录的内容，支持刷新、打开本地 USB 文件夹和清空 Outbox。
- 文件重名时生成编号名称，不静默覆盖。

**用户流程**

- iPhone → Windows：iPhone 将内容导出至 `USB-Outbox`；连接并信任电脑后，在 Apple Devices/iTunes 中从 iphone2win 文件共享复制到 Windows。
- Windows → iPhone：在 Apple Devices/iTunes 中把文件放入 iphone2win 的 `USB-Inbox`；iPhone 在 USB 页面查看目录内容。

**验收标准**

- 无网络时仍可通过 Apple 文件共享完成双向交换。
- 目录创建或文件导出失败时显示本地错误，不自动回退到网络传输。
- 剪贴板为空时不生成空文件。

### 5.5 Windows 自动 USB 传输（P0）

**前置条件**

- Windows 已安装 Apple Devices 或 iTunes 所提供的 Apple Mobile Device 驱动；
- 本地或便携包中存在所需 `libimobiledevice` 工具；
- iPhone 已通过 USB 连接并信任电脑；
- iPhone 已安装当前 bundle id 对应的 iphone2win 应用并开放文件共享。

**需求**

- 按“应用旁 `tools/libimobiledevice/`，其次系统 `PATH`”的顺序查找工具，不自动下载。
- 检测无工具、无设备、单设备、多设备、未信任、已配对、应用 Documents 不可用等状态。
- 仅通过 AFC/HouseArrest 访问 iphone2win Documents。
- “从 iPhone 拉取”将远端 `USB-Outbox` 文件保存到 Windows `USB-Inbox`。
- “推送选定文件”将发送页已选文件准备后写入远端 `USB-Inbox`。
- 外部进程必须使用参数列表和超时控制，不通过 shell 字符串拼接执行。

**验收标准**

- 每种设备/驱动/信任状态均给出下一步可执行提示。
- 已信任的单台 iPhone 能完成推送和拉取。
- 命令返回成功但 AFC 输出包含失败时，操作仍判定为失败。
- 本地同名文件使用安全的新名称保存；不得越过目标目录。
- USB 工具缺失时，局域网、二维码和手动 USB 功能继续可用。

### 5.6 Windows 便携版（P1）

**需求**

- 基于 Windows release 构建生成便携 ZIP、启动脚本和可用时的单文件 EXE。
- `tools/libimobiledevice/` 存在时复制到便携包，不存在时仅提示且继续打包。
- 运行时解压到临时目录并启动 `iphone2win.exe`。

**验收标准**

- 包含和不包含 USB 工具的两种产物均可启动。
- 单文件 EXE 生成流程等待打包程序退出并校验文件存在。
- 构建产物、工具二进制和本机配置不纳入源码版本控制。

## 6. 信息架构与交互

主界面包含四个一级入口：

- 接收：本机接收状态、接收二维码和相关设置；
- 发送：内容选择、附近设备和二维码分享；
- USB：自动 USB 操作、手动目录、文件导出和状态提示；
- 设置：本地传输、接收和应用设置。

桌面端支持将文件或目录拖入应用并自动切换到发送页。所有长时间传输均应展示进行状态，按钮在操作期间避免重复触发。

## 7. 隐私与安全要求

- 正常启动、发现、发送和接收不得连接第三方公网服务。
- 不包含分析、崩溃上报、广告、购买或捐赠 SDK。
- 局域网设备信息只用于本地发现与直连，不发送到公网。
- 二维码上传默认使用 HTTP 以避免 iPhone 浏览器的自签名证书问题；在不可信网络中应提示用户关闭自动接收或启用 PIN。
- 浏览器页面必须同源且无远程脚本、字体、图片或统计资源。
- USB 设备标识仅在本机寻址时使用，日志默认不显示完整标识。
- 每次发布前必须通过 `scripts/privacy_audit.ps1` 或等效审计。

## 8. 兼容性与依赖

- Flutter 版本：3.38.10；
- Windows：Flutter Windows 桌面构建支持的环境；
- iOS/iPadOS：需要本地网络权限；USB 文件共享依赖 iOS Documents 和 Apple File Sharing；
- 自动 USB：依赖 Apple Mobile Device 驱动和兼容的 `libimobiledevice` Windows 工具；
- 当前自动 USB 使用现有 iOS bundle id `org.localsend.localsendApp` 定位文件共享容器，bundle id 迁移不属于本阶段。

## 9. 质量要求

- `flutter analyze` 无错误；
- `flutter test` 全量通过；
- Windows debug 构建成功；发布时 Windows release 和便携打包成功；
- 涉及 Rust 时对应 `cargo test` 通过；
- iOS 相关变更在 macOS/Xcode 环境完成构建或真机验证；
- 新增路径具备单元测试或源码保护测试，覆盖 URL、网页资源、命令参数、状态解析、路径安全和功能入口保留；
- 不采集线上使用数据，产品质量通过自动化测试、手工验收和用户主动反馈评估。

## 10. 发布验收清单

- 完成 iPhone ↔ Windows 局域网双向文件传输；
- 完成扫码上传、扫码下载和文本复制；
- 完成手动 USB 双向文件交换；
- 在真实 Windows、真实 iPhone、有效驱动和 USB 工具环境中完成自动推送/拉取；
- 验证无工具、无驱动/设备、未信任、多设备和应用容器不可用的提示；
- 验证同名文件、空目录、空剪贴板、拒绝、PIN 错误和中断场景；
- 隐私审计、Flutter 分析、全量测试和 Windows 构建全部通过；
- README、CHANGELOG、CONTRIBUTING 和本 PRD 与实际行为一致。
